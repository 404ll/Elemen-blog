# SSE 写作骨架：已经带了事件 ID，为什么还需要去重？

这是一份写作工作稿。先保留问题之间的关系，再逐段补上自己的解释。每次只写一个转折，口语也可以；下面的提示不是需要一次答完的题目。

原始材料：

- [前端面试口述与 SSE 基础讨论](https://chatgpt.com/share/6ab00507-abf8-83e8-be15-5137837878d1)
- [断点续传、去重与完整业务建模](https://chatgpt.com/share/6ab004f6-4020-83e9-866e-c864714843af)

下文除明确标注的原话外，均为对问题的整理，不把 AI 的回答当作作者已经掌握的结论。

## 1. 我知道带 ID 重连，但还说不清“续”的是什么

**对话中的问题：** 你表示大概理解断点续传的过程，但仍不太能解释这个概念。

这一节只沿着一个例子讲，不急着定义所有术语：

```text
服务端已经产生：118、119、120、121、122
客户端成功处理到：120
随后断网，再次连接
```

**由你补写：** 客户端重连时告诉服务端什么？服务端要拿出什么？如果服务端只有一个数字 ID，没有保存事件内容，会发生什么？

> 我的解释：
>
> ……

这一段写清后，再决定要不要补充 SSE 的基础定义。基础知识只保留支撑这个问题所需的部分。

## 2. 既然只补发后面的事件，为什么还需要去重？

**对话中的质疑：** 如果客户端提供的是最后已处理的 ID，后端为什么还会把那条事件发两次？

这是文章的主要转折。保留你的疑问，不直接跳到“至少一次投递需要幂等”的结论。

先把两种情况摆在一起：

| 情况 | 已生效的业务处理 | 恢复后可用的游标 |
| --- | --- | --- |
| 正常恢复 | 已处理到 120 | 120 |
| 异常恢复 | 120 的效果已保存 | 119 |

**由你补写：** 服务端分别会从哪条开始补发？第二种情况重复的原因是什么？

> 我原来的疑问是：……
>
> 我现在把这两种情况分开理解：……

**校对边界：** 单纯断网不一定阻止本地代码更新游标。描述第二种情况时，用“处理效果已经持久化，但游标保存前进程崩溃”等明确场景。若状态和游标都只在内存，刷新后两者都丢失，就不能直接套用表中的第二行。

## 3. 去重记录和消费游标，分别解决什么？

**对话中的问题：** 你追问 `processedIds` 应该记录什么，并用第二次收到 120 的情况检查解释。

这一节从重复事件对结果的影响入手：

```js
article = "Hello";
article += "Hello";
```

**由你补写：** 上面两种操作各自重复执行会发生什么？“收到过”和“处理成功”能不能混用？如果刷新后去重集合丢了，它还能保证什么？

> 我的解释：
>
> ……

**校对边界：** 不预设每套系统都必须同时保存一个 ID 集合和一个游标。有序消费时，游标可以参与去重；跨崩溃恢复则还要考虑状态与游标如何一致保存。正文先讲清自己的例子，再决定是否需要扩展。

## 4. 我需要把它放回整套业务里

**你的原话：** “我的意思是关于这一整套的业务建模”。

前面的局部函数还没有回答：任务是谁执行的，连接断开后谁保存结果，重新连接时又由谁找回事件。

沿用 AI 写作任务，保留下面这些角色，由你把动作和关系补起来：

```text
Job：这次生成任务
Worker：执行任务
Event Store：保存事件
SSE Endpoint：读取并传输事件
Client：应用事件，维护状态与游标
```

**由你补写：** 用户开始生成，看到第一段后断网，Worker 又产生两条事件，用户回来。按时间顺序把这件事讲完。

> 我的解释：
>
> ……

写完后，从自己的解释中提炼一句最值得记住的话，作为文章结尾。暂时不用预写标准答案。

## 留给后续校对的问题

这些可以在主线写完后逐个讨论，不必全部塞进本篇正文：

- 页面刷新后，旧内容与游标如何一起恢复？
- 查完历史再开始订阅，中间的新事件会不会漏掉？
- 原生 EventSource 记录的 ID，与应用确认处理成功的位置是否相同？

已按本骨架完成正文：`content/frontend/sse-job-event-replay-model.mdx`。本文件保留为问题线索和写作过程记录。
